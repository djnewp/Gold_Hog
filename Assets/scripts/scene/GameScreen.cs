using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameScreen : scene_base
{
    // Start is called before the first frame update
    public override void Init()
    {
        base.Init();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
